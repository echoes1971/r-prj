version: '3'
services:
  rprj-web:
    environment:
      SITE_TITLE: ":: R-Project Dev ::"
      RPRJ_ADMIN_PASS: pass
      RPRJ_SKIN: lcars
      MYSQL_PASSWORD: cippa
    volumes:
      - ./php:/var/www/html
    #ports:
      #- 5000:80
  rprj-db:
    environment:
      MYSQL_ROOT_PASSWORD: cippa
