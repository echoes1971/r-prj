
const app_cfg = {
    endpoint: "http://localhost:8080/jsonserver.php"
    // This path is where is stored the react-app, i.e. /myapp/
    ,root_path: "/"
    ,dark_theme: false
};

export { app_cfg }
